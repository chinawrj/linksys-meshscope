--- usr/bin/btjnap
+++ usr/bin/btjnap
@@ -25,2 +25,6 @@
 ACTION=`echo $ACTION | sed -e "s/X-JNAP-Action[ ]*:[ ]*//"`
 AUTH=`echo $AUTH | sed -e "s/X-JNAP-Authorization[ ]*:[ ]*//"`
+
+if [ "$ACTION" = "http://linksys.com/jnap/nodes/meshscope/SteerToParent" ]; then
+    exec /usr/bin/meshscope_parent_steer -u "$AUTH" -j "$JSON"
+fi
